ZERO WARRANTY IS PRESENTED FOR ANYTHING IN THIS REPOSITORY. USER ASSUMES ALL RISK OF USE.

Dependencies:
- Internet Connection to download packages via apt
- snmpd.conf file (see example in snmpd config)
- snmpd.conf in same directory as dependency_install.sh
- update_script.sh in same directory as dependency_install.sh

